import { AcronymParserPipe } from './acronym-parser.pipe';

describe('AcronymParserPipe', () => {
  it('create an instance', () => {
    const pipe = new AcronymParserPipe();
    expect(pipe).toBeTruthy();
  });
});
